using UnityEngine;

namespace Classes
{
    public class Human : Person
    {
        public Human(float maxHealth, float moveSpeed, Transform transform) : base(maxHealth, moveSpeed, transform) { }
    }
}
